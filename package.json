{
  "name": "verigent-mcp-server",
  "version": "0.7.14",
  "description": "MCP server for Verigent — AI agent verification & counterparty due diligence. Check who you're transacting with, carry your own VG credential, flag bad actors.",
  "keywords": [
    "mcp",
    "ai-agents",
    "verification",
    "trust",
    "verigent"
  ],
  "author": "Verigent <support@verigent.ai>",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "verigent-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --no-warnings tests/egress.test.mjs && node --experimental-strip-types --no-warnings tests/copy.test.mjs && node --experimental-strip-types --no-warnings tests/state.test.mjs && node --experimental-strip-types --no-warnings tests/resume.test.mjs && node --experimental-strip-types --no-warnings tests/format.test.mjs && node --experimental-strip-types --no-warnings tests/content.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Verigent-AI/mcp-server"
  },
  "homepage": "https://verigent.ai"
}
